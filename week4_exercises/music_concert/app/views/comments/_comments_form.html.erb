<%= form_for [@concert, @comment] do |f| %>
	 <div>
		<%= f.label :comment %>
		<br />
		<%= f.text_area :comment %>
	</div>
	<div>
		<%= f.label "Posted by" %>
		<br />
		<%= f.text_field :name %>
	</div>

	<br>
	<div>
		<%= f.submit %>
	</div>
<% end %>